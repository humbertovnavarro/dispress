name: Test build

on:
  push:
  pull_request:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          path: tests
      - name: Install dependencies
        run: yarn
      - name: Test build
        run: yarn test

name: Deploy to self-hosted
concurrency: production

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: yarn
      - name: Test build
        run: yarn test
      - name: Build bot
        run:  yarn build
      - name: Run bot
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: yarn start

{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "lerna run dev --stream",
    "stop": "lerna run stop",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "build": "lerna run build",
    "start": "lerna run start --stream",
    "install": "lerna bootstrap",
    "prepare": "prisma db push"
  }
}
